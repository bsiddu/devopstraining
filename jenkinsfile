node ('master')
{
stage 'checkout'
{
    checkout scm
}

stage 'build'
{
    withEnv(["PATH+MAVEN=${tool 'maven3'}/bin"]){
    withEnv(["PATH+JDK=${tool 'jdk8'}/bin"]){
      
    sh "mvn clean verify"
}
}
}
}
